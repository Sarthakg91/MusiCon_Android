<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivityPam"
    android:background="@android:color/background_dark"
    >

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select the image that best captures how you feel right now."
        android:id="@+id/pamTextView"
        android:gravity="center_horizontal"
        android:textSize="20sp"
        android:paddingBottom="20sp"
        android:textColor="@android:color/secondary_text_light_nodisable"

        />
    <GridView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/pamGridView"
        android:numColumns="4"
        android:layout_below="@+id/pamTextView"
        />
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/pamGridView"
        android:text="Refresh Images"
        android:paddingBottom="16dp"
        />
</RelativeLayout>
